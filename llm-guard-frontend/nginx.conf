server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    # Health check endpoint
    location = /health {
        access_log off;
        default_type text/plain;
        return 200 "healthy\n";
    }

    # Route-change ping endpoint — logs every SPA navigation
    location ~ ^/log-nav {
        default_type text/plain;
        return 200 "logged\n";
    }

    # SPA fallback routing
    location / {
        try_files $uri $uri/ /index.html;
    }
}